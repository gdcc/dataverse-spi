<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.gdcc.spi</groupId>
        <artifactId>parent</artifactId>
        <version>2.1.0-M1</version>
    </parent>
    
    <groupId>io.gdcc</groupId>
    <artifactId>dataverse-spi</artifactId>
    <!-- For now, keep all the module versions in lockstep. Can be diversified later. -->
    <!-- <version>x.y.z.-SNAPSHOT</version> -->
    <name>Dataverse SPI - All-in-One Package</name>
    <description>One-stop-shop for Dataverse plugins containing all contracts and infrastructure</description>
    
    <!--
        Even with no source code, still package as JAR!
        As a POM-only artifact, it only acts like a BOM/parent, not roping in dependencies for users!
    -->
    <packaging>jar</packaging>
    
    <!--
        This module aggregates all the artifacts to form a single distribution package.
        Users use the transitive dependencies to build Dataverse plugins.
    -->
    <dependencies>
        <dependency>
            <groupId>io.gdcc.spi</groupId>
            <artifactId>meta</artifactId>
        </dependency>
        <dependency>
            <groupId>io.gdcc.spi</groupId>
            <artifactId>core</artifactId>
        </dependency>
        <dependency>
            <groupId>io.gdcc.spi</groupId>
            <artifactId>export</artifactId>
        </dependency>
        
        <!-- Collection all dependencies from the shaded modules here, as otherwise Javadocs cannot be compiled -->
        <!-- MAKE SURE TO USE provided AS SCOPE OTHERWISE THESE WILL BE SHADED, TOO! -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.json</groupId>
            <artifactId>jakarta.json-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>jakarta.ws.rs</groupId>
            <artifactId>jakarta.ws.rs-api</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <!-- Make the shaded artifact the main output of this submodule -->
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <!-- Merge the "-sources" jars of meta/core/export into dataverse-spi-<version>-sources.jar -->
                            <createSourcesJar>true</createSourcesJar>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <!-- Generate Javadoc from the sources of the aggregated modules -->
                            <includeDependencySources>true</includeDependencySources>
                            <dependencySourceIncludes>
                                <dependencySourceInclude>io.gdcc.spi:*</dependencySourceInclude>
                            </dependencySourceIncludes>
                            <!-- This module contributes no sources itself, don't complain about that -->
                            <skip>false</skip>
                            <failOnWarnings>false</failOnWarnings>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>